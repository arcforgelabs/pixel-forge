# Intent

Make Pixel Forge the fastest and most usable way to visually edit a real running app, including Pixel Forge itself, while keeping the real agent runtime visible, targetable, and controllable. Agent Deck transparency is the operator viewport; Pixel Forge is the truthful visual-context and handoff surface. Pixel Forge should own visual context assembly and request brokering, not a fake headless session layer. If the operator surface is confusing, visually noisy, or unpleasant to use, the product is not done.

## Primary Operator Loop

1. The user can see active agents clearly in Agent Deck and switch between them quickly.
2. The user can select the exact live element or region they mean inside Pixel Forge.
3. Pixel Forge can bundle that truthful selected context, any live inspection handle it has, and the user's written instruction into one handoff.
4. That handoff reaches the intended working agent/session without hiding the real runtime behind an opaque internal abstraction.
5. The target agent can inspect and act on that exact selected surface truthfully.

Everything else is subordinate to improving this loop and making it feel clear, direct, and low-friction in daily use.

# Goals

1. Live Editor sends selected-element context, attachments, and written instruction into a persistent real endpoint agent session without losing continuity on server restart.
2. The user can route that bundled handoff to a specific working agent/session while keeping that target visible, controllable, and natively interactive in Agent Deck.
3. Agent Deck remains the default operator viewport for scanning and switching between active agents, and those visible sessions must stay native endpoint sessions while Pixel Forge remains the visual context and handoff surface.
4. Every live-edit request is inspectable and reproducible from a disk-backed request pack instead of giant pasted blobs or process memory.
5. Users can pull visual context from multiple preview tabs into one prompt within the active Live Editor thread while keeping multiple thread-scoped chats and selection workflows truthful inside the same project.
6. Pixel Forge can launch a faithful sibling Pixel Forge mirror runtime for self-edit loops without colliding with the controller instance or diverging from the UI being fixed.
7. Selected-element context must stay truthful across DOM, document-like PDF, and canvas-like preview surfaces, and the working agent must be able to inspect the frozen selected state without replaying the browser path manually.
8. Pixel Forge preview interaction must grow as a real tool system, with selection as the first tool rather than a permanent special case, so future capabilities can ship without sprawling across Live Editor state and shell glue.
9. Working agents must use Pixel Forge-forged preview evidence truthfully instead of inferring or inventing live behavior from repo code when the user already navigated and selected the real target surface.
10. Pixel Forge's operator surface must stay intuitive, visually simplified, and pleasant enough that small UX cleanups count as real product progress when they preserve truthful behavior and capability.
11. The Pixel Forge alpha lane must converge Pixel Forge and Agent Deck toward one shared workstation kernel with one chat identity, one binding model, and one event stream, while the stable installed Pixel Forge and standalone Agent Deck remain untouched until that lane proves itself.
12. The selection tool should feel intuitive across supported formats so basic wording feedback, visual edits, and future document-like targets follow one understandable interaction model instead of substrate-specific surprise behavior.

# Requirements

## Root Requirements

### Runtime Truth
- `REQ-R-001:` Live Editor must run against a persistent Agent Deck endpoint session, not an in-process per-server-run Claude session manager.
- `REQ-R-002:` One Pixel Forge Live Editor thread maps to one persistent Agent Deck endpoint session until the user explicitly starts a fresh thread, except for a one-off automatic migration when Pixel Forge upgrades a legacy live-edit binding onto the current runtime contract. Multiple Live Editor threads may run simultaneously, but each thread must keep exclusive ownership of its Agent Deck session lane instead of sharing or stealing another thread's endpoint session. Manual interaction inside that same visible endpoint session must remain part of the same underlying thread continuity rather than forking into an untracked side session.
- `REQ-R-003:` Screenshot bootstrap may stay on the direct Claude CLI path until the Agent Deck-backed replacement is proven and faster.
- `REQ-R-004:` The long-term agent session control plane should converge on a structured upstream runtime/protocol wrapped by Pixel Forge rather than grow bespoke repo-local orchestration when the upstream path better serves the primary operator loop.
- `REQ-R-005:` Upstream runtime adoption must sit behind a thin Pixel Forge adapter boundary so versions can be pinned deliberately and local glue can be deleted as upstream capabilities replace it. In the alpha lane, imported Agent Deck source may live inside this repo behind an intentional boundary so the shared kernel can be built locally without mutating the stable standalone install.
- `REQ-R-006:` The default live-edit handoff model must bind directly to one chosen Agent Deck session rather than route through a rebinding broker abstraction. Once that real backend lane exists, Pixel Forge must stop implying that the agent can still be switched in place.
- `REQ-R-007:` When the operator starts a fresh Live Editor thread without selecting an existing suitable target session, Pixel Forge must keep that thread as an unbound draft until the first send or another flow legitimately needs a real isolated lane. The draft must keep its intended initial agent, and the first real bind must honor that choice while creating a new isolated clone-backed Agent Deck session for the thread.
- `REQ-R-007A:` Clone-backed first bind remains the default for a fresh Live Editor chat, but the operator must be able to choose canonical-root first bind explicitly when they want to work directly in the workspace root. That choice must be available in the new-chat/create-bind flow without requiring hidden API knowledge, and Pixel Forge must keep the chosen workspace mode truthful in the saved chat/session metadata.
- `REQ-R-008:` Agent Deck should remain the operator-facing session identity layer, and the default visible Agent Deck pane for a Live Editor target must stay the real endpoint agent session rather than a custom proxy shell that replaces native operator behavior.
- `REQ-R-009:` Pixel Forge must provide a first-class clone-promotion command that merges isolated clone-backed session work back into the canonical root and can optionally commit, push, stage, apply, and clean up from that same operator flow.
- `REQ-R-010:` The alpha lane must run as a side-by-side semantic-version/runtime/install lane that can be launched and installed without replacing the stable Pixel Forge runtime or the stable standalone Agent Deck runtime. Its live mutable state must live in its own top-level alpha-owned root instead of nesting under the stable Pixel Forge state root.

### Context Transport
- `REQ-C-001:` Each Live Editor request must be written to a disk-backed request pack before it is sent to the agent runtime.
- `REQ-C-002:` Request packs must include the user request, selected-element context, and any attachments in readable files instead of inline prompt blobs.
- `REQ-C-003:` Request packs must be retained long enough to debug the current loop, then cleaned up automatically to avoid unbounded growth.
- `REQ-C-004:` The dispatch unit must keep the user's written instruction, the selected-surface context, and any frozen/live inspection references bundled together all the way to the target agent/session. When the request is asking for captured evidence like a screenshot, Pixel Forge must also surface that captured evidence back into chat as a first-class artifact instead of only paraphrasing it.
- `REQ-C-005:` Live Editor handoff framing must be bootstrap-once and delta-after. The first handoff to a new or rebound endpoint session may include stable Pixel Forge setup context and a thread-level brief, but subsequent turns on the same visible endpoint session must keep the operator prompt intact and send only direct turn artifact refs plus turn-specific context unless recovery or migration requires a fresh bootstrap.
- `REQ-C-006:` Explicit slash-skill directives in the user request must be promoted into the typed turn payload and request-pack mirror as exact skill names, and the live dispatch transport must preserve the operator's exact skill invocation instead of wrapping it in explanatory skill prose. Pixel Forge must not bury requested skills in wrapper narration or optional flavor text.
- `REQ-C-007:` Live Editor must expose the runtime-discovered skill homes and use the installed skill folders they contain to drive slash-skill autocomplete in the chat composer. Pixel Forge may also surface source libraries for operator visibility, but autocomplete must be driven by on-disk installed skill trees rather than a separate metadata registry.
- `REQ-C-008:` Request packs remain the durable human-readable artifact surface for each turn, but they are not the primary live transcript or control plane once a lane is warm. The shared workstation event stream owns live turn progression and output truth for Pixel Forge-managed turns, and the shared kernel must also carry one typed per-turn request payload for prompt text, selected items, live-preview state, attachment refs, and requested skills so the disk files are durable projections of the turn, not its only machine-readable source.
- `REQ-C-009:` The chat composer must not explode large pasted context into an unreadable raw draft blob by default. When pasted text crosses the large-context threshold, Pixel Forge must convert it into a first-class `paste` attachment artifact, keep a truthful inline reference token at the insertion point, and still let the operator remove that artifact quickly without manually scrubbing the whole pasted body.
- `REQ-C-010:` Sent transcript/history must preserve attachment reference position and keep large pasted context inspectable. The operator should be able to see where a pasted/image/file artifact sat in the prompt, and large pasted artifacts must remain expandable from chat history instead of becoming either hidden transport-only state or giant permanent inline blobs.

### Transparency
- `REQ-T-001:` Pixel Forge must surface the Agent Deck session identity it is using for a Live Editor thread without demoting the persisted chat/thread identity into a raw Agent Deck session category.
- `REQ-T-002:` Pixel Forge must preserve chat streaming when possible by adapting the underlying agent transcript instead of falling back blindly to black-box waits.
- `REQ-T-003:` Agent Deck must remain the primary transparency surface for active agent sessions in this workflow. Pixel Forge may augment it, but it must not hide the real target session behind an internal-only broker abstraction, and closeout/cleanup flows must not claim a source session is gone while that same source session still survives visibly in Agent Deck.
- `REQ-T-004:` A live-edit request must be able to target a specific intended working agent/session truthfully instead of disappearing into an opaque backend pool.
- `REQ-T-005:` If an upstream ACP runtime is wrapped beneath Agent Deck, Agent Deck must store and expose the mapping between its own session id and the upstream runtime ids needed for backend control, while keeping the Agent Deck session id as the primary operator-facing handle and leaving the intermediate coordination layer invisible by default in the normal Agent Deck workflow.
- `REQ-T-006:` The alpha lane must expose one machine-consumable shared event stream/log for session lifecycle and output so Pixel Forge and Agent Deck can observe the same underlying activity without per-surface polling being the primary truth.
- `REQ-T-007:` Pixel Forge-managed Live Editor turns must append typed workstation events directly into that shared event log as the turn happens. The minimum typed set is `turn_started`, `turn_status`, `turn_chunk`, `turn_completed`, and `turn_failed`.
- `REQ-T-008:` Adopted or manually-driven sessions must land on the same shared event log through native primary events, and any tool lane with a truthful native transcript or hook surface must be promoted from session-level visibility into typed turn events on that same log. Session-level `session_status` and `session_output` events may remain only for lanes that do not yet expose a proven turn-granular surface, and synthesized `activity` snapshots may remain only as compatibility fallback for chats that still lack any native primary history.

## Support Requirements

### Repo and State
- `REQ-S-001:` Repo-level operational truth lives in `SPECS.md`; runtime notes in `AGENTS.md` and `CLAUDE.md` must agree with it.
- `REQ-S-002:` Pixel Forge broker metadata belongs in a small persistent store; bulky artifacts belong on disk outside that metadata path.
- `REQ-S-003:` Workspace binding, preview target selection, and generated-output destination must stay separate in the product model. Do not collapse them into one misleading "project" field.
- `REQ-S-004:` Workspace/project metadata and resumable Live Editor session linkage must persist in the Pixel Forge backend store instead of browser localStorage.
- `REQ-S-005:` Project/workspace metadata, Live Editor thread/session linkage, explicit chat-to-live-session bindings, staged update records, and workstation event records must live in a shared Pixel Forge control-plane store visible to the controller and mirror runtimes. Runtime sandboxes may isolate browser state, logs, and build artifacts, but they must not silently fork this shared product metadata. In the alpha lane that same control plane must back both the Pixel Forge shell surface and the Agent Deck kernel surface.
- `REQ-S-006:` One Live Editor thread must have one default writable workspace root plus one thread-scoped editor state: preview tabs, active target URL, viewport/tool mode, selector/history state, and chat state all belong to that same lane. Unless the operator explicitly retargets or loads a different mirror candidate, request packs, default direct edits, committed selections, default self-edit mirror launch, and supported workspace-preview candidate discovery/launch should all follow that same bound workspace and thread lane instead of browsing process-global localhost state.
- `REQ-S-007:` The durable subset of Live Editor thread state needed to reopen a lane after shell restart, reinstall, or controller refresh must persist in the shared control-plane store. Persist thread-owned tab descriptors, active target URL, viewport/panel mode, and lane binding metadata there; reacquire runtime-only browser/proxy handles on reopen instead of storing stale handles as truth.
- `REQ-S-008:` Pixel Forge must keep one default operator profile in the shared control-plane store and use it on ordinary app launch to reopen the last active project, active mode, active Live Editor lane, and default-agent preference instead of dropping the operator into a blank session unless they explicitly clear that state.
- `REQ-S-009:` If a persisted Live Editor lane points at an Agent Deck session that no longer exists, Pixel Forge must detach that dead backend binding while preserving the lane, workspace pointer, and durable editor state. Dead bindings may not silently hide or delete the lane, and a later reattach attempt must prefer that saved lane workspace over creating a fresh clone when the saved workspace still exists.
- `REQ-S-010:` The Projects sidebar order is stable operator truth, not a recency heuristic. Opening a project, focusing a chat, receiving activity, or refreshing bindings must not reshuffle projects or chats based on active or recent state.
- `REQ-S-011:` Anonymous detached root `draft-*` lanes and empty legacy root shell rows are implementation detail, not operator-visible chat rows. Pixel Forge must not surface or reopen those placeholders as first-class chats, and any `draft-*` lane that becomes truthfully bound to a real Agent Deck session must be promoted to explicit `chat-*` identity before it surfaces. Explicit chats and truthfully bound lanes are the visible unit.

### Git Hygiene
- `REQ-G-001:` Request packs created inside a target repo must not pollute the user’s normal git status.
- `REQ-G-002:` Scratch generated code defaults to `.pixel-forge/generated/` inside the bound workspace unless the user explicitly chooses a repo-relative output path.
- `REQ-G-003:` Clone workspaces under `.agents/` must not pollute the parent repo's normal git status and must not be copied into staged controller-update snapshots.

### Preview Targets
- `REQ-P-001:` Live preview targets may be any URL Pixel Forge can proxy or alias, not just localhost dev servers. For supported local workspaces, Pixel Forge should also be able to discover likely preview candidates inside the bound workspace, recommend one deterministically, launch it in an isolated workspace-owned runtime, and expose it through a stable workspace-bound preview URL even when the underlying transport port has to move.
- `REQ-P-002:` Direct code edits still require a writable workspace even when the preview target is a remote or third-party website.
- `REQ-P-003:` Authenticated staging and production targets must keep login state inside Pixel Forge by binding each browser session to its own proxy target and upstream cookie jar.
- `REQ-P-004:` Proxy setup from the web app must use credentialed backend calls so the browser-scoped proxy session cookie is actually established before the iframe loads.
- `REQ-P-005:` Pixel Forge preview must run through the native shell's embedded Chromium surface for both localhost and third-party targets. The browser-only web build is a service/debug fallback, not a supported Live Editor preview surface.

### Multi-Tab Preview Context
- `REQ-M-001:` The Live Editor preview layer must support multiple browser-style preview tabs inside one project/chat instance. Adding a tab must not start a new chat thread.
- `REQ-M-002:` Each preview tab must keep its own proxy/browser session state, including upstream cookie/auth state, so switching tabs restores that tab’s browsing context.
- `REQ-M-003:` Selected elements must carry explicit preview-tab identity and resolved source URL so the agent can reason about cross-tab comparisons.
- `REQ-M-004:` The element context builder must group selections by preview tab/source, including tab and URL metadata, while still sending one unified prompt to the agent.
- `REQ-M-005:` Switching preview tabs must not clear the unified selected-element list. The active iframe only renders overlays for selections that belong to the active tab.
- `REQ-M-006:` Pixel Forge must not enforce a small fixed preview-tab cap. Practical limits are driven by runtime/browser resources, not a hardcoded product limit.
- `REQ-M-007:` In the native shell path, preview tabs must render inside Pixel Forge itself rather than bouncing the user out to separate external browser windows.
- `REQ-M-008:` When selections span multiple preview sources, the short dispatch prompt must not collapse them into one misleading preview target or deploy instruction. The request-pack source grouping is the truth, and the dispatch prompt may only summarize it faithfully.

### Selection Engine
- `REQ-X-001:` Pixel Forge must choose the selection strategy automatically from the live preview substrate instead of exposing selector-mode choice as a default user workflow.
- `REQ-X-002:` DOM-like targets must preserve semantic selectors and DOM excerpts; canvas-like targets must preserve spatial region anchors and visual evidence instead of fake DOM references.
- `REQ-X-003:` Selection identity must be stable by Pixel Forge selection id, not only by xpath, so renumbering, removal, and cross-tab reconciliation stay truthful.
- `REQ-X-004:` Visual overlays must reattach only when the selected target's page/view identity and substrate match the current live preview state.
- `REQ-X-005:` While select mode is active on DOM-like targets, pressing `Left Ctrl` with the pointer held still must cycle the proposed hover target upward through ancestor containers, and any mouse movement must reset that cycle back to the immediate hovered target.
- `REQ-X-006:` While select mode is active on an already selected DOM-like target, holding `Ctrl+Shift` over that selection must preview promotion to the next ancestor container in a distinct transitional state, and clicking must replace that saved selection in place instead of removing and re-adding it.
- `REQ-X-007:` Pixel Forge must support undo and redo for selection-engine mutations so selection add/remove/clear/promote mistakes can be reversed without rebuilding the whole prompt manually.
- `REQ-X-008:` Document-like surfaces such as PDFs must expose substrate-native semantic targets through the common selection tool. Ordinary clicks should favor meaningful text/content units before falling back to a spatial region capture.
- `REQ-X-009:` PDFs must support first-class text-range selection as the target architecture. Click-drag highlight over one or more words or lines should commit one selection element with the combined text, stable page/range anchors, and frozen evidence instead of many line hits or a loose region box.
- `REQ-X-010:` PDF region capture is a secondary spatial fallback, not an equal default to text. The operator UI and selection tunnel must distinguish `pdf text` from `pdf region` clearly so agents and users do not confuse wording edits with area-based feedback.

### Tool System
- `REQ-L-001:` Pixel Forge preview interaction must be modeled as named tools with a shared controller/registry, not as one-off booleans and ad hoc event handling embedded directly in Live Editor UI code.
- `REQ-L-002:` The current selection tool must live inside that tool system as tool `v1`, not as a permanent special case that future tools have to work around.
- `REQ-L-003:` Each tool must own its input handling, transient hover/proposal state, committed artifact state, overlay rendering, and request-pack/tunnel serialization contract.
- `REQ-L-004:` Tool state must distinguish transient proposed targets from committed artifacts so hover previews, promotion, undo/redo, and future tool composition stay predictable.
- `REQ-L-005:` Tool shortcuts, modifier chords, and focus handoff must be routed through a central tool input layer instead of being solved separately inside each tool implementation.
- `REQ-L-006:` Default tool behavior should stay automatic when the substrate makes the right choice obvious, but advanced sub-modes and alternate probing strategies may exist behind explicit chords or advanced controls.
- `REQ-L-007:` Pixel Forge must record which tool and sub-mode produced each committed artifact so the working agent can interpret the selection tunnel truthfully.
- `REQ-L-008:` The user-facing tool surface should converge toward a compact persistent tool palette/rail, not a scattering of special-case buttons across individual panes. Operator chrome should prefer clarity, simplification, and low visual noise over redundant controls or decorative status labels when capability is unchanged.

### Agent Selection Tunnel
- `REQ-U-001:` Each Live Editor request pack must include a structured frozen selection tunnel artifact in addition to human-readable selected-elements markup.
- `REQ-U-002:` Pixel Forge must expose agent-facing inspection paths for selected live surfaces. The minimum path is the frozen local selection tunnel. The first live path is a live-preview context reference into the already-running preview session, and the target path is direct agent-native attach into that same session without replaying login, navigation, or view reconstruction.
- `REQ-U-003:` Canvas-like and other spatial selections must carry screenshot evidence into the request pack so the agent receives the visual state Pixel Forge forged, not an inferred substitute, and dispatch instructions must require the agent to state any remaining inspection limitation explicitly instead of guessing.
- `REQ-U-004:` Agent-facing Pixel Forge operations must converge on the installed `pixel-forge` CLI plus a dedicated `using-pixel-forge` skill. Repo-local wrappers may exist for Pixel Forge self-development, but they must dispatch to the same canonical command definition. Do not fork agent behavior across a second parallel CLI surface unless the canonical CLI proves insufficient.
- `REQ-U-005:` For a targeted live-edit handoff, Pixel Forge must be able to pass the chosen agent enough truth to act on the exact selected surface: frozen evidence at minimum, live-preview context when available, direct attach when proven, and explicit indication when only the frozen lane exists. When the user explicitly requests real live/CDP attach proof and attach hints exist, controller-captured BrowserView state is orientation context only and may not be treated as a successful substitute for the requested attach proof.
- `REQ-U-006:` Document-like selections must preserve the real source document URL, page number, semantic text when available, spatial bounds when region fallback is used, frozen preview evidence, and any discoverable workspace provenance such as generator or source-file hints. Agents must be able to inspect the already-loaded document truthfully even when the raw generating source is unavailable.
- `REQ-U-007:` The target document-inspection lane is exact replay into the already-loaded document: Pixel Forge should be able to reopen or refocus the saved page/area/range and highlight it for the agent. Until that exists, the request pack and live-preview context must state that the current PDF lane is frozen-evidence plus live-context only, not exact replay.

### Self-Edit Runtime
- `REQ-E-001:` Pixel Forge must be able to launch a sibling Pixel Forge target runtime for a compatible workspace, with isolated ports, runtime sandbox paths, and managed-browser profile paths. For Pixel Forge-owned local targets, documented/default ports are transport preferences rather than the user-facing preview identity: the visible preview URL must stay stable per workspace/source root while the bound backend port may float underneath to avoid clashes.
- `REQ-E-002:` The default self-edit target must be a faithful mirror of Pixel Forge's real controller UI and startup flows, including the workspace selector and deeper self-target launch paths. Safety belongs to isolated runtime/state boundaries and backend/runtime policy interception, not to front-end neutering of the target surface. Faithful does not require preserving redundant chrome, stale labels, or other operator-facing clutter when behavior and access stay truthful.
- `REQ-E-002A:` Self-edit must be preview-first. A completed Pixel Forge self-edit request should load into the current chat's primary mirror preview by default before the user decides to promote that same build into the controller, while still remaining a distinct mirror build artifact that can coexist with other mirror candidates when explicitly opened.
- `REQ-E-003:` A Pixel Forge self-edit request must not restart or replace the active Pixel Forge controller before the current live-edit stream finishes.
- `REQ-E-004:` Self-edit activation should stage repo changes first and leave install/restart activation to an explicit post-run step rather than cutting off the in-flight broker session.
- `REQ-E-005:` The installed controller path must keep at least one rollback build so a broken self-update can be reverted quickly.
- `REQ-E-006:` Pixel Forge update publication must use one shared model with two scopes: canonical-root completions publish into the shared staged controller-update lane, while clone-backed self-edit completions publish preview-only updates scoped to the bound isolated session.
- `REQ-E-007:` A staged controller update must freeze a source snapshot at stage time so applying it cannot accidentally pick up later unrelated working-tree drift.
- `REQ-E-007A:` When the source repo has a usable local git ref, Pixel Forge should be able to freeze the staged controller update from that exact local ref instead of the mutable filesystem working tree.
- `REQ-E-008:` Applying a staged controller update must relaunch the shell against the restored project/mode/preview context so desktop-shell code changes can take effect without manual relaunch choreography.
- `REQ-E-009:` If Pixel Forge keeps a lower-fidelity dev-target path for rapid iteration, that path must be explicit and must not replace the faithful mirror target as the default self-edit surface.
- `REQ-E-010:` Mirror preview builds must be versioned local artifacts keyed by their frozen source snapshot/runtime root so multiple mirror candidates can stay open in separate tabs without overwriting each other.
- `REQ-E-011:` The primary mirror-launch control must deterministically relaunch the latest mirror for the active Live Editor thread's effective isolated workspace source instead of browsing project-wide mirror-history choices from the toolbar.
- `REQ-E-011A:` Pixel Forge self-edit preview must be bound to an isolated clone workspace. If no clone-backed workspace is already selected or bound, Pixel Forge must create or require selection of one before launching the mirror.
- `REQ-E-012:` Mirror build artifacts belong in Pixel Forge state outside the repo so they do not pollute git status or require git-ignore churn in the workspace.
- `REQ-E-013:` Applying a staged controller update must hand off to a detached update runner and close the active controller shell before install/restart begins. The update progress surface may be a separate dedicated updater window, but the controller being replaced must not remain responsible for its own update lifecycle.
- `REQ-E-014:` Pixel Forge must keep one truthful semantic controller version and runtime identity and surface the running controller identity inside Settings: running version, staged controller-update version/status, runtime source/layout, and enough runtime metadata to tell whether the active controller is actually the expected build. Lane-specific names, hosts, install roots, preview partitions, and agent-facing CLI prompts must derive from that runtime identity configuration instead of separate hardcoded per-surface strings. Dismissing the header update notice must not hide the apply path from Settings.
- `REQ-E-015:` Controller-update operations must have one canonical CLI-first surface: `pixel-forge controller-update stage|status|apply|clear`. UI and desktop-shell affordances may wrap that flow, but they must call the same canonical surface instead of maintaining a private apply path.
- `REQ-E-016:` By default, controller-update staging and apply must source from the canonical project root or an exact local git ref from that canonical root. Clone workspaces under `.agents/` are preview/edit sandboxes unless an explicit noncanonical override is given.
- `REQ-E-016A:` Clone-backed self-edit completions must keep preview refresh separate from controller promotion. They may offer reload of the updated clone mirror, but they must not stage shared controller-update records until the clone is explicitly promoted back into the canonical root.
- `REQ-E-017:` Pixel Forge must keep its own user-scoped managed skill home in shared state rather than inside the mutable app install tree so reinstalls do not wipe the installed-skill surface.

### Deploy-Aware Feedback Loop
- `REQ-D-001:` Pixel Forge must detect whether the preview target is remote (not localhost/127.0.0.1) and surface that awareness in the completion flow.
- `REQ-D-002:` When the agent completes a code edit against the active preview target and that target is controlled by the workspace, the request pack and dispatch prompt must instruct the agent to apply the update to that preview target before treating the task as done.
- `REQ-D-003:` For local/dev preview targets, the agent must rebuild, restart, or reload the service serving that exact preview URL/path using the repo's normal workflow instead of stopping at file edits.
- `REQ-D-004:` For repo-controlled remote preview targets, the agent must deploy using whatever deployment process the workspace provides. No standardized deploy convention — the agent infers the deploy method from the workspace.
- `REQ-D-005:` If the active preview target is external or not controlled by the workspace, the agent must say that explicitly and skip deployment or reload instead of silently doing nothing.
- `REQ-D-006:` After agent completion on a remote target, the chat UI must show a "Refresh Preview" action that cache-busts and reloads the iframe to the current path without destroying the proxy session or auth state.
- `REQ-D-007:` A manual "Refresh Preview" button must always be available in the toolbar as a universal fallback regardless of target type.

# Current Limiting Factor

- `[active]` Universal native context ingress plus live browser attach is now the current limiting factor. Alpha now has truthful turn assembly, typed `turn-input.json`, request-pack mirrors, selection tunnels, live-preview context, prompt-first dispatch, direct native resumed Claude/Codex warm turns, and a cleaner attachment-token composer that keeps large pasted context as first-class artifacts instead of draft blobs. The remaining gap is no longer basic prompt cleanup; it is one durable universal bridge from Pixel Forge context into native agent sessions and back into the already-running browser session.
- Why it is the limiter: the operator surface is materially cleaner now that large pasted context, files, and images can sit in the draft as inline references with real attached artifacts, but the underlying transport is still split by tool. Claude still relies on direct `@file` refs, Codex has a real native `--image` lane plus compact file refs, and live inspection of the prepared browser state still depends on tool-specific follow-through instead of one universal Pixel Forge-owned browser attach contract that any upstream agent can consume.
- Smallest complete unit to attack it: promote the current internal attachment-token model into one thin universal transport contract, then pair it with a brokered live browser attach lane that emits the same CDP/MCP handle to Claude, Codex, and future agents. Prove one installed-alpha Claude turn and one Codex turn that both receive the same truthful turn bundle and can attach to the same warm browser session through the same Pixel Forge-emitted handle.
- Immediate proof target: in the installed alpha shell, the operator prompt reaches both tools unchanged, inline paste/image/file references resolve to real artifacts, `request.md` remains a mirror rather than the conversational surface, Codex still receives native `--image` inputs where available, and both tools can consume the same live browser attach manifest truthfully instead of needing divergent browser-control choreography.

# Current Proof Status

- `[validated]` The alpha lane now has a side-by-side semantic version and runtime identity (`2.0.0-alpha.1`) that defaults to `pixel-forge-alpha`, `pixel-forge-alpha-shell`, `pixel-forge-alpha.localhost`, and `~/.pixel-forge-alpha` instead of the stable controller lane. Basis: `scripts/alpha-env.sh` is now the lane-identity root for install/run metadata, `install.sh` propagates that identity into the installed service and launchers, request-pack prompts derive CLI/host identity from the active runtime config, and the desktop shell/runtime defaults resolve through the isolated alpha identity instead of hidden stable fallbacks.
- `[validated]` The alpha lane now has one dedicated source branch of record: `dev/pixel-forge-alpha`. Basis: the current alpha runtime/docs/install identity now align to `pixel-forge-alpha`, the branch is pushed to `origin/dev/pixel-forge-alpha`, and future alpha work can continue from a normal repo checkout or worktree without depending on the original bootstrap clone path.
- `[validated]` The alpha lane now carries an intentional in-repo Agent Deck foundation boundary and can point this lane at that local runtime instead of the separately installed Agent Deck binary. Basis: `foundations/agent-deck/` is present in this lane, `scripts/agent-deck-alpha.sh` builds and runs that vendored foundation, the install path now bundles it, and the API bridge resolves commands through the alpha Agent Deck runtime helper.
- `[validated]` The alpha lane now owns a full integrated Agent Deck runtime home under `~/.pixel-forge-alpha/agent-deck` with the alpha-owned profile slug `alpha` instead of the old `workstation-v2` profile. Basis: the alpha env, launcher scripts, installed service, runtime bridge defaults, and migration helper now resolve Agent Deck through the alpha profile; existing migrated roots promote `agent-deck/profiles/workstation-v2` to `agent-deck/profiles/alpha` before the runtime uses it.
- `[validated]` Alpha state now migrates out of the old nested `~/.pixel-forge/workstation-v2` root into the dedicated top-level alpha root without mutating stable v1 state, and the old nested root is removable after successful alpha verification. Basis: startup/install/runtime entrypoints now run `ensure_alpha_state_root.py`, runtime config defaults to `~/.pixel-forge-alpha`, the migration helper copies durable alpha state one-way from the legacy subroot, and it intentionally skips transient `runtime/` noise so stale pid/log files are not imported into the new alpha root.
- `[validated]` The alpha lane now has a real second alpha-owned Agent Deck visual surface that can run without the TUI and be launched from the same installed Pixel Forge alpha lane. Basis: vendored Agent Deck now exposes `web-standalone`, the controller runtime owns `agent_deck_surface.py` plus `/api/agent-deck-surface` and `pixel-forge-alpha agent-deck-surface ...`, and the desktop shell can open that surface in a second Pixel Forge window.
- `[validated]` The alpha lane now also has a dedicated `Agent Deck (alpha)` terminal-app launcher that opens the real vendored Agent Deck TUI against the alpha-owned home/profile without touching the stable standalone Agent Deck install. Basis: the controller CLI now exposes `agent-deck-tui open|run`, install now ships the separate launcher `pixel-forge-agent-deck-alpha` plus its own desktop entry and WM class, and the alpha TUI path resolves through the vendored Agent Deck runner with `profile=alpha`, `homeDir=~/.pixel-forge-alpha/agent-deck`, and `dbPath=~/.pixel-forge-alpha/pixel-forge.db`.
- `[validated]` Existing Pixel Forge chat identity remains first-class in the alpha lane while live Agent Deck sessions are bindings, not a competing user-facing category. Basis: persisted `sessions.thread_id` remains the current chat-id compatibility surface, unmatched Agent Deck sessions are adopted into saved chat rows before they surface, and lookup can resolve from `agent_deck_session_id` back to the owning chat.
- `[validated]` The integrated Agent Deck surface now overlays shared Pixel Forge chat identity onto its session menu instead of showing only raw Agent Deck session titles. Basis: the vendored web surface reads the shared Pixel Forge DB through `PIXEL_FORGE_DB_PATH`, annotates matching Agent Deck sessions with `chatId` and `chatTitle`, and renders that shared chat identity as the primary row label.
- `[validated]` The alpha lane now persists explicit chat-to-live-session bindings separately from the durable chat rows. Basis: `chat_session_bindings` backfills existing links, keeps live session metadata out of the durable chat row, and supports truthfully detaching a dead Agent Deck session without deleting the chat.
- `[validated]` Pixel Forge now consumes a shared workstation event stream for observed attached or adopted chats instead of relying on per-surface polling as the primary truth. Basis: `workstation_events` now carries typed Pixel Forge-managed turn events, native Agent Deck `session_status`/`session_output` events, and compatibility activity snapshots, `/api/projects/{project}/chats/{chat}/events` emits named SSE events, the frontend subscribes with named `EventSource` listeners, and targeted backend/frontend tests cover typed-turn replay, native session-event hydration, and snapshot fallback.
- `[validated]` Side-by-side alpha dev launch is proven without replacing the stable Pixel Forge runtime. Basis: `./start-dev.sh` in this clone booted API on `127.0.0.1:7201` and web on `pixel-forge-alpha.localhost:5273`, both responded successfully, and logs landed under `~/.pixel-forge-alpha/runtime/logs/`.
- `[validated]` Live Editor thread metadata and durable tab-restore state now persist outside process memory. Basis: Pixel Forge stores session linkage plus sanitized thread editor state in the shared SQLite-backed local state database.
- `[validated]` The shared control-plane store now keeps a default operator profile pointer for ordinary reopen restore. Basis: the backend persists active project/mode/thread profile state plus the default-agent preference in SQLite, and the frontend resumes from it on normal app launch instead of relying only on controller-update bootstrap relaunch state.
- `[validated]` Persisted Live Editor lanes now survive dead Agent Deck attachments truthfully. Basis: the frontend detaches unavailable Agent Deck ids from saved lane metadata instead of dropping the lane, and the backend clears missing Agent Deck bindings out of the shared session/thread stores while preserving workspace and editor state.
- `[validated]` Reattaching a detached clone-backed Live Editor lane now reuses that lane's existing workspace instead of minting a second clone, and long-running Codex/non-JSONL turns now stay attached until real Agent Deck completion instead of failing on the CLI's 10-minute wait path. Basis: `ensure_agent_deck_session` now relaunches detached lanes against their saved workspace when it still exists, and the live-editor dispatch path now uses Agent Deck's ready-gated `session send` to deliver the prompt, then polls the lane independently with periodic status heartbeats until the session actually settles.
- `[validated]` Live Editor request context now lands on disk as request packs under `.pixel-forge/requests`. Basis: the backend writes request packs before dispatch.
- `[validated]` Pixel Forge now dispatches Live Editor requests into Agent Deck sessions instead of spawning direct `claude -p` processes for that path. Basis: `/ws/live-editor` uses the Agent Deck bridge.
- `[validated]` Pixel Forge surfaces Agent Deck session identity in the Live Editor UI. Basis: the frontend persists and displays Agent Deck session metadata.
- `[validated]` Pixel Forge still has a version-pinned ACPX bridge in the repo for structured runtime experiments and legacy/runtime control paths. Current runtime: `acpx@0.3.1`. Basis: end-to-end websocket smokes previously bound ACPX-backed Agent Deck sessions, wrote ACPX ids into thread/request-pack state, and streamed ACPX tool activity and agent output through the real Live Editor route.
- `[validated]` Live Editor now defaults back to real native Agent Deck endpoint sessions for new and migrated Claude/Codex threads instead of surfacing the custom ACPX shell as the operator pane. Basis: fresh Agent Deck probes returned `tool=claude`, `command=claude`, and a real `claude_session_id`; `agent-deck session send` answered normally; tmux capture showed the native Claude Code terminal UI launched as `claude --session-id ...`.
- `[validated]` ACPX wrapper sessions are no longer part of the normal target-session picker, and legacy Live Editor threads now auto-migrate off ACPX wrapper bindings onto native Agent Deck sessions on first reuse. Basis: project session listing now excludes ACPX wrapper commands, reused-thread probing rebound a real wrapper-backed thread onto an existing native Claude session, and a wrapper-with-normal-title smoke renamed the wrapper to `...-acpx` before launching a native replacement with the original title.
- `[validated]` Screenshot-style evidence requests now surface captured selection images back into assistant chat bubbles as first-class artifacts. Basis: frontend store regression coverage proves screenshot requests attach the captured selection image to the assistant message.
- `[validated]` Live Editor dispatch is now continuity-aware: first handoff to a session uses the fuller Pixel Forge bootstrap framing, while later turns on the same visible session switch to a shorter delta prompt that assumes earlier setup context still holds. Basis: the dispatch builder now distinguishes bootstrap vs continuation turns using existing thread/session continuity and emits the shorter turn framing unless a new/rebound session requires re-bootstrap.
- `[validated]` Live Editor request packs now separate stable session rules from per-turn request delta by writing a thread-level `session-brief.md` and keeping later `request.md` files continuity-aware. Basis: the request-pack writer now persists a stable thread brief under `.pixel-forge/threads/<thread-id>/session-brief.md`, bootstrap request packs point at that brief explicitly, and continuation request packs keep only the new turn request plus delta-specific references.
- `[validated]` New Pixel Forge-created Live Editor chats now start as draft-first lanes inside the reconciled chat model instead of provisioning a real Agent Deck session up front. Basis: chat creation now persists a draft thread row with `draftAgentType`, the frontend no longer auto-provisions a live lane for a brand-new chat, and the same saved draft can be reopened without minting a second lane before first send.
- `[validated]` The first real bind for a Pixel Forge-created Live Editor chat still creates an isolated Agent Deck clone workspace under the project `.agents/` tree while keeping the canonical project root as project identity. Basis: `ensure_agent_deck_session` still launches fresh lanes in clone mode, persisted thread/session state keeps the bound workspace path, project session discovery accepts clone workspaces under `.agents/`, and Live Editor request packs write into that bound workspace once the lane exists.
- `[validated]` Fresh Live Editor chats can now choose clone-backed or canonical-root first bind truthfully before the first send, and that workspace choice locks once the real lane exists. Basis: project chat creation now persists `draftWorkspaceMode`, the draft chat composer exposes a `Clone`/`Root` choice only while the lane is still unbound, frontend first-send payloads now forward `workspace_mode`, and `ensure_agent_deck_session` honors `root` on first bind while still preferring clone by default.
- `[validated]` One Live Editor thread now has one default writable workspace root, and the default self-edit mirror source follows that bound workspace deterministically. Basis: thread/session state persists the bound workspace path, the primary mirror-launch control resolves the isolated workspace source from the bound clone or selected isolated session, and the toolbar no longer browses project-wide mirror history.
- `[unvalidated]` Agent Deck can surface the real native endpoint Claude/Codex session while ACPX remains an invisible coordination layer behind that same session. Basis: this is still the intended end architecture, but the pinned ACPX runtime currently resumes ACP-created sessions only and does not yet prove a shared-session attach path into the native Agent Deck pane.
- `[validated]` Direct bind to a chosen Agent Deck session is now the default Live Editor handoff model, with thin create-and-bind behavior only when Pixel Forge actually needs a new working session. Basis: the backend still enforces target-session binding, fresh draft chats keep only intended agent state until first send, and the first send path resolves either the chosen existing target or a newly created session without pretending post-bind agent mutability exists.
- `[validated]` Agent Deck keeps its own operator-facing session ids while upstream runtime ids remain mapped backend metadata instead of replacing the operator UX identity. Basis: the Live Editor UI/API still target Agent Deck session ids, while `acpx_agent`, `acpx_session_name`, `acpx_record_id`, and `acp_session_id` now persist in Live Editor thread state and request packs.
- `[validated]` Pixel Forge now separates workspace selection, preview URL, and generated output policy in the selector flow. Basis: the selector now binds a workspace, accepts any preview URL, and defaults scratch output to `.pixel-forge/generated/`.
- `[validated]` Project/workspace metadata and resumable Live Editor session linkage now persist server-side in SQLite under the active shared-state root instead of browser localStorage. Basis: recent projects, preview URL history, and Live Editor session metadata now load from backend APIs, and the alpha lane defaults that DB to `~/.pixel-forge-alpha/pixel-forge.db`.
- `[validated]` Shared Pixel Forge metadata now lives in a control-plane store that is visible to controller and mirror runtimes instead of being snapshotted into isolated mirror DB copies. Basis: the shared SQLite DB and staged controller-update state now resolve through the shared control-plane root while mirrors keep isolated runtime sandboxes for browser/build state.
- `[validated]` Pixel Forge now maintains browser-scoped proxy sessions with upstream cookie jars for authenticated targets. Basis: `/config/app-proxy` issues a local proxy-session cookie and the app proxy reuses per-session upstream clients instead of one global stateless target.
- `[validated]` Pixel Forge now maintains multiple mounted preview tabs inside one Live Editor thread, with each tab bound to its own proxy session and active URL state. Basis: browser smoke loaded `https://field.arcforge.au/` and `https://claude.ai/new` in separate tabs and kept the active URL bar on the real target URL rather than the internal proxy URL.
- `[validated]` Cross-tab element selection now survives tab switches and stays unified in one project-level context list. Basis: browser smoke selected one element from the Claude tab and one from the Field tab, then the Elements pane showed both selections together with their source URLs.
- `[validated]` Multi-source request artifacts preserve grouped source truth, and the broker prompt no longer collapses them into one singular preview target. Basis: request-pack inspection showed mixed Pixel Forge + Claude selections grouped correctly, and the dispatch prompt builder now summarizes multi-source selections instead of emitting a single remote-target line for them.
- `[validated]` Pixel Forge now has a native desktop-shell path (`apps/desktop`) that embeds real Chromium-backed preview tabs inside the app chrome. Basis: Electron shell proof loaded a live remote page into the preview pane, switched it to `https://example.com/`, enabled selecting, and the shared Elements context incremented inside the Pixel Forge UI.
- `[validated]` The product preview path now treats embedded Chromium as the only supported Live Editor surface. Basis: the launcher defaults to the shell, `Save & Edit Live` now hands URLs straight to Live Editor state instead of configuring the proxy, and the browser-only Live Editor path now blocks with a shell-required message.
- `[validated]` Pixel Forge can now launch a sibling Pixel Forge target runtime for self-editing without sharing controller ports, DB state, or managed-browser profile state. Basis: the backend local-target launcher allocates isolated runtime settings and the controller opens the returned target URL inside the existing preview-tab system.
- `[validated]` The default self-edit launch path now uses the mirror lane instead of defaulting to the dev/HMR target lane. Basis: the primary mirror-launch control launches `runtime_kind=mirror`, and the dev/HMR lane remains explicit rather than the default.
- `[validated]` The primary mirror-launch control now deterministically relaunches the effective isolated workspace mirror instead of browsing project-wide mirror-history choices from the toolbar. Basis: preview launch now resolves the bound clone workspace, falls back to a selected isolated session path, or creates a new isolated session before rebuilding the mirror from that source root.
- `[unvalidated]` Pixel Forge self-edit requests now stay controller-safe through completion instead of restarting the active broker mid-stream. Basis: request-pack and dispatch-prompt guardrails exist, but the full self-edit loop has not yet been re-proven end to end.
- `[validated]` Clone-backed self-edit preview now stays isolated from shared controller-update staging. Basis: clone-bound completion notices now publish a preview-only snapshot keyed to the clone workspace/session, `Load Updated Preview` loads from that frozen clone snapshot into the current chat's primary mirror by default, and controller promotion remains explicit through clone promotion back into the canonical root followed by `pixel-forge controller-update stage|apply`.
- `[validated]` Staged controller updates now apply from a frozen source snapshot instead of the mutable live repo working tree. Basis: stage-time snapshot smoke proved the staged snapshot excludes transient heavy paths, `show` returns the frozen payload, and `clear` removes both the pending record and snapshot.
- `[validated]` Staged controller updates can now freeze from an exact local git ref instead of the mutable filesystem when the operator wants deterministic commit-based promotion. Basis: the controller-update path now accepts `gitRef`, resolves it locally with Git, archives that exact commit into the staged snapshot, and records the resolved `commitHash`.
- `[validated]` Clone workspaces under `.agents/` no longer pollute parent-repo git status or staged controller-update snapshots. Basis: the root `.gitignore` now ignores `.agents/`, and staged snapshot creation explicitly excludes `.agents/` alongside other transient heavy paths.
- `[validated]` Controller-update operations now have one canonical CLI-first surface, and the installed launcher plus repo-local wrapper both dispatch to that same command definition. Basis: `pixel-forge controller-update stage|status|apply|clear` now lives in one shared Python CLI, the legacy top-level aliases are compatibility shims onto that parser, the desktop shell now hands update apply off through that same CLI, and the update smoke now runs through `controller-update apply` instead of invoking the detached runner directly.
- `[validated]` Controller-update staging now rejects clone workspaces under `.agents/` by default unless the operator passes an explicit noncanonical override. Basis: stage-time policy now resolves the canonical project root, refuses clone-backed project paths by default, and still allows a deliberate override when the operator truly wants a noncanonical install source.
- `[validated]` The detached controller update runner now waits for the restarted controller to report the staged version before it declares the update complete. Basis: the runner reads the expected version from the staged source root and the update smoke now proves the version transition instead of stopping at first HTTP success.
- `[validated]` The installed controller lane now keeps a real rollback build and can restore it after a staged update install. Basis: isolated smoke proved base install -> frozen staged update -> runtime version transition -> rollback -> original version restored.
- `[validated]` Applying a staged controller update now relaunches the shell back into the same project/mode/preview context. Basis: the shell writes bootstrap state to disk before install/restart, then relaunches Electron so updated shell code can take effect without a manual close/reopen dance.
- `[unvalidated]` `pixel-forge clone promote` is now the right one-shot operator lane for clone-backed self-edit cleanup and controller promotion. Basis: the command now wraps `agent-deck clone finish`, canonical-root commit/push, and optional controller-update stage/apply, but it has not yet been re-proven end to end on a live clone with the full commit/push/apply/cleanup sequence.
- `[unvalidated]` Settings now surfaces the running controller version, staged controller-update status, runtime source/layout, and ACPX-capability identity, and it retains the apply path after the header notice is dismissed. Basis: the controller/runtime metadata path now includes runtime root/layout plus ACPX bridge presence and the installed controller can now be refreshed directly from the canonical root, but the full shell interaction proof still needs a fresh manual smoke after the latest refresh.
- `[validated]` Sibling target runtimes can now surface the shared workspace-selector affordance and recursive self-launch controls inside the isolated target runtime. Basis: target mode now exposes a selector opener in-app and no longer suppresses the primary mirror-launch control inside the target runtime.
- `[unvalidated]` Mirror-target fidelity is high enough that fixing the target UI fixes the controller UI. Basis: the mirror launcher now targets the current runtime artifact and seeds isolated state from the current runtime, but end-to-end UI proof against the startup dialog mismatch has not yet been re-run after that change.
- `[unvalidated]` Mirror runtimes are fully shell-capable when Pixel Forge runs inside Pixel Forge, including nested preview ownership and update affordances. Basis: the default self-edit target is now a faithful mirror build, but recursive shell capability inside that mirror runtime has not yet been completed.
- `[unvalidated]` Fresh nested Pixel Forge mirrors still surface the session-target affordance even before a live-editor session is bound, but that affordance now lives in the sidebar project chat list, the chat-based Settings target control, and the draft-chat composer agent chooser instead of a raw-session retargeting flow. Basis: the project sidebar and Settings both consume reconciled project chats, fresh chats are created as drafts before binding, and visible Agent Deck sessions are adopted into chats before they appear there, but the fresh mirror/sidebar interaction has not yet been manually re-smoked after the draft-first shift.
- `[unvalidated]` Sidebar project chat rows now expose rename and delete actions, and deleting a clone-backed Agent Deck chat with recorded activity offers a closeout lane before destructive removal. Basis: the sidebar now renders per-row hover menus backed by rename/delete/closeout APIs and the live mirror proves the `Rename`/`Delete` affordance is present, but the full destructive delete and closeout lifecycle has not yet been manually re-smoked end to end.
- `[unvalidated]` Live Editor thread state is now lane-scoped instead of project-global: messages, selected elements, undo/redo history, preferred Agent Deck target, preview tabs, active target URL, viewport mode, and editor panel state all stay with their own thread, and Pixel Forge refuses to reuse an Agent Deck session that another thread already owns. Basis: the frontend store now keeps those facts per thread, focused store tests prove selector/target/editor isolation, and the backend/store now reject duplicate Agent Deck session claims, but the concurrent multi-thread flow has not yet been manually exercised end to end.
- `[validated]` Claude Code is now the persistent default agent for new Pixel Forge chats unless the operator explicitly changes that preference. Basis: shared profile state now persists `default_agent_type`, the store hydrates that preference on load, and fresh chat creation plus isolated-session creation both default to that saved value.
- `[validated]` A brand-new unsent Live Editor chat can now choose its initial agent truthfully before first send, and that choice locks once the first real backend lane exists. Basis: draft thread editor state now persists `draftAgentType`, the chat composer exposes the chooser only while no bound or targeted real session exists, and first-send lane creation honors the draft choice instead of retargeting an existing raw session model.
- `[unvalidated]` Live Editor threads can now restore their preview tabs after shell restart or reinstall refresh without pretending old browser handles still exist. Basis: the backend session store now keeps sanitized editor-state snapshots, the shared profile state points ordinary app launch back at the last active project/mode/thread, and active tabs reacquire their runtime handles on reopen, but the full desktop-shell restore flow still needs a fresh manual smoke.
- `[unvalidated]` Embedded mirror previews no longer assume the full shell desktop bridge exists inside preview BrowserViews. Basis: runtime bootstrap and controller-update reads are now capability-gated against the preview preload's partial bridge and web/desktop static checks pass, but the Electron blank-preview regression has not yet been re-smoked visually.
- `[validated]` Self-edit now separates clone-preview loading from controller promotion in the product model. Basis: clone completions publish preview-only updates that reload the primary mirror tab for that workspace by default from a frozen clone snapshot, while canonical-root completions still stage the shared controller update and promotion remains an explicit post-promotion action against the canonical root.
- `[validated]` Mirror launch now follows the existing chat's real backing context instead of inventing a hidden safety migration. Basis: existing root-backed chats launch from the latest staged controller snapshot when present or fall back to the current controller runtime, existing clone-backed chats launch from the latest clone preview snapshot when present or fall back to their bound workspace, and only brand-new draft chats still default to clone creation.
- `[validated]` Adopted Agent Deck chats are no longer treated as blank Pixel Forge drafts just because Pixel Forge did not create them. Basis: fresh-chat creation now only blocks on truly unbound empty drafts, selecting a bound-but-message-less chat hydrates the attached Agent Deck session's current status/output into the lane, and busy attached sessions now queue live-edit sends through Agent Deck instead of failing on the default ready wait.
- `[unvalidated]` Mirror and dev runtimes no longer auto-restore persisted Pixel Forge local-target tabs on startup, preventing runaway nested self-mirror recursion inside target runtimes. Basis: non-controller `LiveEditorPane` runtimes now skip local-target auto-restore and leave the tab metadata intact for manual reload, but this exact shell-level recursion fix has not yet been re-smoked visually after the patch.
- `[unvalidated]` Target runtimes now stay one layer deep for Pixel Forge itself. Basis: non-controller runtimes refuse to reopen the originating Pixel Forge workspace, block nested Pixel Forge target URLs and self-mirror launch actions, but still keep ordinary external-app preview controls available; this exact mirror-shell behavior still needs a fresh manual smoke.
- `[validated]` Mirror candidates are now versioned local runtime artifacts rather than one mutable mirror instance. Basis: mirror instances are keyed by source snapshot/runtime root, can coexist in separate tabs, and can be listed/selectively reopened.
- `[validated]` Desktop preview input ownership now separates visible preview attachment, focused surface, and armed tool state. Basis: the desktop bridge now exposes preview input state, preview show/focus are distinct paths, ordinary DOM mutations no longer re-show the BrowserView through the overlay guard, and chat focus reclaim goes through the shell bridge instead of BrowserView side effects.
- `[unvalidated]` Pixel Forge preview interaction is now partially restructured toward a first-class tool system with selection carried as explicit tool state, but the full shared controller/registry is not complete yet. Basis: selection now flows through explicit desktop/shell tool state instead of one local boolean, but not every tool concern has been extracted into a shared controller layer.
- `[validated]` Live Editor request packs now include a structured `selection-tunnel.json` artifact and can expose it back through a local API/CLI path. Basis: request-pack smoke created the tunnel file, registered it in the manifest, and the backend now serves `/api/live-editor/selection-tunnel`.
- `[validated]` Pixel Forge now stores screenshot evidence alongside selection context so non-DOM selections can carry frozen visual state into the request pack. Basis: the shell selection bridge now captures bounded image crops during selection and the frontend includes them as request-pack attachments.
- `[validated]` Embedded PDFs in alpha now route through the internal viewer and commit selections against the real source document instead of the Chromium PDF viewer chrome. Basis: the installed alpha shell loaded the exact `quote.pdf` file into `/internal/pdf-viewer`, the viewer surface now ignored header/chrome clicks, text clicks resolved as `pdf text`, empty page clicks resolved as `pdf region`, and the saved selection source URL stayed on the real `file:///...quote.pdf` document rather than the internal helper route.
- `[validated]` Selected DOM targets can now be promoted upward in place without losing their selection slot, and the shell preview shows that promotion as a yellow transitional preview before commit. Basis: Electron smoke selected a child button, showed `Promote to div#parent` with yellow badge state on `Ctrl+Shift`, then emitted `browser-element-updated` with the same selection id retargeted to the parent xpath.
- `[validated]` Selection add/remove/clear/promote mutations now support undo and redo in the controller state. Basis: store regression tests prove replace/remove/clear restore the exact selection order and ids through undo/redo.
- `[unvalidated]` Automatic selector routing is good enough across real hybrid DOM/canvas/WebGL apps, not just synthetic cases. Basis: implemented in the shell selection engine, but not yet hammered against a wider hostile sample.
- `[unvalidated]` PDF selection is intuitive enough for ordinary wording feedback on messy real documents. Basis: the current alpha lane now distinguishes `pdf text`, `pdf text range`, and `pdf region`, suppresses viewer-chrome selections, carries stable page/range metadata through the selection tunnel, and can reveal a restored PDF selection back into the warm viewer, but the installed shell still needs a fresh visual smoke on real documents before that lane can be called proven.
- `[unvalidated]` PDF text-range selection now collapses a click-drag highlight into one semantic selection artifact with combined text, stable page/range anchors, and frozen evidence instead of many single-line hits or fallback regions. Basis: the internal PDF adapter now records `pdfTextRange` anchors plus `pdfSelectionKind=text-range`, the desktop bridge commits that artifact on drag-highlight mouseup, and request-pack/live-preview artifacts preserve the same range metadata through the shared selection contract.
- `[unvalidated]` Saved PDF selections can now reveal the exact saved page/area or text range back into the already-loaded internal viewer during active-tab selection restore instead of staying frozen-only metadata. Basis: the PDF adapter now resolves and reveals saved page/range anchors through the shared `applySelections` path, but that reveal lane has not yet been visually re-smoked in the installed shell.
- `[validated]` Ancestor-cycle hover selection now moves upward through visible DOM ancestors while the pointer stays still and resets back to the direct hovered target on movement. Basis: shell smoke on nested markup showed child `1/3`, parent `2/3`, then reset to the child after mouse movement.
- `[validated]` Dispatch and delivery rules now survive the thinner handoff contract as structured turn data instead of long wrapper prose. Basis: `turn-input.json` now carries artifact refs plus preview-delivery metadata, `request.md` mirrors the same truth concisely, and the prompt-first dispatch block now sends the operator text plus direct `@file` refs for the live turn bundle instead of reverting to the older memo wrapper.
- `[validated]` Pixel Forge now provides a dedicated `using-pixel-forge` skill with the current CLI commands instead of expecting agents to infer the CLI/tunnel workflow. Basis: the skill documents the canonical `pixel-forge` controller-update and clone-promotion commands, and requested use of that skill now surfaces as exact skill names in the typed turn payload and request-pack mirror while the live dispatch stays prompt-first instead of burying that skill request in wrapper prose.
- `[validated]` Explicit slash-skill requests are now first-class handoff data instead of buried prose. Basis: request-pack writing extracts requested skills into a dedicated `## Skills` section and manifest metadata, `turn-input.json` carries the same skill list, and the dispatch transport now preserves the operator's exact slash-skill text while avoiding the older explanatory wrapper contract.
- `[validated]` Pixel Forge now scans real runtime skill homes on disk instead of maintaining a separate installed-skill registry file. Basis: the backend now discovers skills directly from the Pixel Forge, Claude, Codex, and OpenClaw skill folders, the install script creates the shared-state Pixel Forge skill home, and `/api/skills` exposes that runtime-scanned state to the UI.
- `[validated]` The Projects sidebar and advanced Settings retargeting now run on one reconciled Pixel Forge chat model instead of hand-merging persisted lanes with raw Agent Deck session rows in the UI. Basis: the backend now exposes `/api/projects/{project}/chats` plus chat creation, which reconcile or adopt visible Agent Deck sessions into Pixel Forge chats, and both the sidebar and Settings now consume that chat list directly while raw Agent Deck data stays behind the scenes.
- `[validated]` Sidebar project/chat ordering now stays stable instead of jumping on active or recent state, detached root `draft-*` placeholders no longer reopen as visible chats, attached legacy `draft-*` lanes are promoted to explicit `chat-*` identities before they surface, and empty detached legacy root shells are pruned instead of lingering invisibly in state. Basis: backend project/session/chat reads now preserve stable order, frontend merge paths replace in place instead of prepending, anonymous root draft placeholders are filtered out of visible project session/chat lists, attached draft identities are promoted on save/startup, empty detached adopted/legacy root shells are deleted once they have no saved state, and targeted regression tests cover ordering plus hidden-draft/promotion/pruning behavior.
- `[validated]` Missing-workspace isolated Agent Deck rows no longer resurface as project chats in alpha, and closeout truth now requires removing any surviving zombie source row instead of stopping at clone-directory cleanup. Basis: project session listing now prunes missing-path non-root sessions through Agent Deck-native removal before surfacing them, the closeout prompt now explicitly requires zombie-row removal plus `agent-deck ls --json` verification, and targeted backend regression tests cover orphan-row cleanup.
- `[validated]` Pixel Forge-managed Live Editor turns now append typed workstation events directly into the shared event plane. Basis: `/ws/live-editor` now records `turn_started`, `turn_status`, `turn_chunk`, `turn_completed`, and `turn_failed` into `workstation_events` as the real send/stream flow runs, the Claude/Codex/ACPX stream helpers mirror emitted payloads into that same log, and the observed-lane store now reconstructs chat state from those named SSE events.
- `[validated]` Native Agent Deck-originated adopted/manual session activity now lands on the same first-class event plane through primary `session_status` and `session_output` events. Basis: the API now tails the alpha Agent Deck `events/*.json` foundation path, correlates native `instance_id` values back to bound chats, appends those session-level events into `workstation_events`, the SSE fallback gate now treats them as primary history, and targeted backend/frontend tests prove ingest plus observed-lane hydration without relying on synthesized `activity` snapshots.
- `[validated]` Off-path/manual Claude Agent Deck sessions now promote native hook and JSONL transcript truth into typed turn events on the shared kernel instead of remaining session-snapshot only. Basis: the API now tails Agent Deck `hooks/*.json`, correlates Claude hook `session_id` values back to bound chats, tails Claude JSONL deltas per upstream session, emits `turn_started`, `turn_chunk`, and `turn_completed` into `workstation_events`, and the observed-lane store suppresses later duplicate session snapshots so the assistant response hydrates once. Targeted backend/frontend tests prove that manual Claude output lands in the bound Pixel Forge lane as one real observed turn.
- `[validated]` Off-path/manual Codex Agent Deck sessions now promote native notify-hook and Codex JSONL transcript truth into typed turn events on the shared kernel instead of remaining session-snapshot only. Basis: the API now tails Agent Deck `hooks/*.json`, reuses the sticky hook-session anchor when completion hooks omit `session_id`, tails Codex `response_item` JSONL deltas from `~/.codex/sessions/...`, and emits `turn_started`, `turn_chunk`, and `turn_completed` into `workstation_events`. Targeted backend tests prove that a manual Codex turn with an empty-id completion hook still lands in the bound Pixel Forge lane as one real observed turn.
- `[validated]` Pixel Forge preview tabs now expose a first real live-preview handoff lane alongside the frozen selection tunnel. Basis: the frontend now sends active preview-tab identity with each live-edit turn, the backend captures `live-preview-context.json` into the request pack, `/api/live-editor/live-preview-context` exposes the same surface through the canonical CLI, and targeted backend/frontend tests prove payload propagation plus request-pack/CLI/context capture.
- `[validated]` Warm-session continuation now carries a first-class per-turn context patch instead of only another request-file reference. Basis: continuation and delta turns now write `context-patch.json` into the request pack, `turn-input.json` carries the same continuity payload, and the prompt-first dispatch transport now forwards that patch through direct artifact refs for the already-running Agent Deck session without replaying the old long wrapper.
- `[validated]` Live-preview context now carries exact CDP attach hints whenever the warm preview substrate can expose them. Basis: browser inspection now captures DevTools browser URL plus page target metadata, live-preview capture/refresh emit `attach_hints` with a canonical `chrome-devtools-mcp --browserUrl ... --slim --no-usage-statistics` command, and targeted backend tests prove those hints propagate into the live-preview payload.
- `[validated]` Pixel Forge now has a canonical live attach proof lane for warm preview reuse. Basis: `turn-input.json` now carries structured attach-proof commands whenever live-preview attach hints exist, `request.md` mirrors the same commands concisely, the CLI writes `attach-proof.json` into the request pack, and the same command mirrors structured proof into the shared workstation event stream as `turn_status`. That lane now also carries canonical project identity through the request-pack manifest and requires an explicit `--via` so clone-backed chats mirror under the right project row and receipts can truthfully name the actual attach mechanism instead of silently assuming `chrome-devtools-mcp`. Explicit live/CDP attach proof requests still disallow controller-browserview success as a substitute when attach hints exist.
- `[validated]` The corrected clone-backed attach-proof receipt lane is now clean end to end in the installed alpha controller. Basis: a fresh installed-alpha Claude rerun completed with `proof_path=cdp-attach`, `attach_proof_cli=succeeded`, and matching `artifact_status=succeeded` / `artifact_via=raw-cdp`, proving that `attach-proof.json` and the shared kernel receipt now agree without the earlier foreign-key failure.
- `[validated]` Pixel Forge-owned local targets now keep a stable workspace-bound preview identity above floating backend ports. Basis: local target records now derive a controller-owned `stable_url` from runtime identity instead of the raw transport port, the Live Editor opens/restores those targets through the stable alias, the controller now proxies alias hosts back to the correct raw target endpoint, and targeted backend/frontend tests cover alias derivation, alias matching, and redirect rewriting.
- `[validated]` Supported workspace-local app previews can now be discovered and launched directly from the bound Live Editor workspace without scavenging localhost manually. Basis: the backend now scans the lane workspace for common `package.json` app candidates, recommends one deterministically, starts it on a free port inside that same workspace boundary, and exposes it through a stable controller-owned alias; the Live Editor toolbar now opens that candidate list and launches the chosen preview into the active lane tab with restore metadata.
- `[validated]` Prompt text, selected items, live-preview state, attachment refs, requested skills, artifact refs, and preview-delivery metadata now have one canonical typed per-turn payload on the shared kernel instead of existing only as request-pack files plus wrapper prose. Basis: request packs now write `turn-input.json`, the manifest records that artifact, `turn_input` workstation events mirror the same payload into the shared kernel, and the dispatch transport now sends the operator prompt first plus direct `@file` refs while treating `request.md` and the other files as durable mirrors instead of the conversational surface.
- `[validated]` Pixel Forge-managed warm turns now prefer direct native upstream delivery instead of Agent Deck key-send whenever the real Claude/Codex session IDs are known. Basis: the live-editor bridge now captures `codex_session_id` alongside `claude_session_id`, Agent Deck `session show` JSON exposes that Codex ID, Claude warm turns dispatch through native resumed Claude CLI with the untouched operator prompt plus native `@file` refs, Codex warm turns dispatch through native resumed Codex CLI with the untouched operator prompt plus native `--image` items and compact context-file paths, and targeted Python/Go tests cover prompt shaping, native dispatch selection, fallback behavior, and the new Agent Deck JSON surface.
- `[validated]` The Live Editor composer now treats large pasted context as a first-class attachment artifact instead of forcing giant raw pasted blobs into the draft. Basis: the composer now converts large paste bursts into `paste` attachments, inserts inline `[Paste #n]`/`[Image #n]`/`[File #n]` reference tokens at the caret, lets backspace/delete or the attachment-chip close affordance remove the whole artifact quickly, preserves those inline references in the sent user transcript, and keeps pasted text expandable from chat history while the request pack persists the same pasted artifact as a plain-text attachment file.
- `[unvalidated]` The selection tunnel gives working agents enough frozen context to avoid replaying third-party auth or deep navigation in practice. Basis: file/API/CLI path now exists, but it has not yet been exercised by a real agent workflow end to end.
- `[validated]` Claude can directly attach to the already-running warm preview session and inspect or control that live target without replaying auth or navigation. Basis: a real March 22, 2026 operator run from the installed alpha controller matched the emitted CDP target id, returned live URL/title/readyState/scrollY/interactive-element/console facts from the attached session, and visibly navigated the warm preview from the jobs list to a quote detail page.
- `[unvalidated]` Off-path Claude hook plus JSONL turn parity is good enough for all real Claude tool flows, not just the common prompt->assistant->stop path. Basis: implemented from observed Agent Deck hook payloads and Claude JSONL structure, but not yet proven across wider tool-heavy or interrupted cases.
- `[unvalidated]` Off-path Codex hook plus JSONL turn parity is good enough for all real Codex tool flows, not just common assistant-message turns. Basis: implemented from observed Codex notify events plus `response_item` JSONL records, but not yet proven across wider tool-heavy or interrupted cases.
- `[validated]` Codex and other non-JSONL native sessions now surface the best truthful in-app transparency currently available instead of heartbeat-only opacity. Basis: Live Editor now tails Agent Deck session output after send, emits incremental assistant chunks when a safe Codex text delta appears, routes progress-only lines into status updates instead of fake transcript text, and still uses real Agent Deck completion as the final turn boundary.
- `[unvalidated]` Request-pack retention limits are the right balance between debuggability and disk usage. Basis: chosen pragmatically for the first working cut.
- `[unvalidated]` Deploy-aware feedback loop: remote target detection, deploy instruction in dispatch prompt, and Refresh Preview button in chat and toolbar. Basis: implemented in backend and frontend but not yet proven against a real staging target.
- `[unvalidated]` Cross-tab dispatch context: one request pack and one agent prompt clearly preserve grouped source metadata for selections gathered from multiple preview tabs. Basis: browser-side tab/session/selection behavior is now proven, but the full dispatch artifact has not yet been inspected.
- `[unvalidated]` Self-edit rebuild resilience: the controller keeps the sibling target tab usable across repeated target rebuilds/restarts. Basis: target launch plumbing exists, but the restart/reconnect loop has not yet been hammered.
- `[validated]` Pixel Forge now has a canonical root verification lane for fast iteration hardening. Basis: `pnpm verify` proves version sync, shell syntax, API/desktop/web checks, isolated install smoke, and staged controller-update apply/rollback smoke before the same lane is wired into CI.
- `[planned]` Screenshot bootstrap will be migrated onto the same session control plane only after the Live Editor loop is proven superior there.

# Open Questions

- `[question]` Should request-pack retention become content-addressed and deduplicated, or is bounded per-request storage sufficient in practice?
- `[question]` Should the second source panel support its own independent deploy-aware refresh, or is one shared refresh sufficient?
- `[question]` When we add behind-the-pixel probing later, should it stay on a separate modifier chord from ancestor cycling or become an explicit advanced selection sub-mode?
- `[question]` Should the eventual tool palette be global to the whole preview workspace, or should some tools only appear when the active substrate/runtime can actually support them?
- `[question]` Should the eventual live-preview deep-inspection lane prefer browser-native CDP attach first with Pixel Forge live-context capture as the fast path, or should Pixel Forge own a richer browser-session API even when CDP attach is available?

# Out of Scope

- Replacing the stable installed Pixel Forge runtime during alpha-lane R&D.
- Replacing the stable standalone Agent Deck runtime during alpha-lane R&D.
- Migrating screenshot bootstrap to Agent Deck before the shared workstation kernel is proven.
- Preserving fake “unified session” semantics between screenshot bootstrap and live editing if they make the backend model less truthful.

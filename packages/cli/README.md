# Pixel Forge Bootstrap CLI

Thin CLI wrapper around the Pixel Forge SDK for screenshot bootstrap tasks.

## Use

```bash
chmod +x pixel-forge-bootstrap
./pixel-forge-bootstrap ./design.png --output ./result.html
```

The command expects `ANTHROPIC_API_KEY` in the environment.

The previous long-form CLI notes were archived to `README-legacy-20260314.md`.
